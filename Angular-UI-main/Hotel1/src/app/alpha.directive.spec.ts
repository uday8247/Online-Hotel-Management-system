import { AlphaDirective } from './alpha.directive';

describe('AlphaDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphaDirective();
    expect(directive).toBeTruthy();
  });
});
